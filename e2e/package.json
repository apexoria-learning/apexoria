{
  "name": "apexoria-e2e",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Playwright end-to-end tests for Apexoria Learning. Isolated from the main app package so the React 19 --legacy-peer-deps tree stays clean. See .github/agents/qa.agent.md for the E2E charter.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report ../test_reports/playwright-html",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
