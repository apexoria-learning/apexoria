# Test Credentials

This app has NO authentication. No login/admin credentials required.

- Public marketing site with a lead capture form.
- Lead form posts to: POST /api/leads (stored in MongoDB collection `leads`).
- Admin leads list: GET /api/leads
- Brochure PDF: GET /api/brochure
